import { Outlet } from 'react-router-dom';

const Layout = () => {
  return (
    <>
      {/* TODO: 필요시 레이아웃 추가 */}
      {/* TODO: 사이드바 추가 */}

      <Outlet />
    </>
  );
};

export default Layout;
