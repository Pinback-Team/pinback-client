import CardEditModal from '@shared/components/cardEditModal/CardEditModal';

const MyBookmark = () => {
  return (
    <div>
      <CardEditModal onClose={() => {}} /> {/* 임시로 추가한 부분 */}
    </div>
  );
};

export default MyBookmark;
